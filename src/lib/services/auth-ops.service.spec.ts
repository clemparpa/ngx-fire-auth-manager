/**
 * auth-ops.service.ts is not unit tested because it is a wrapper for @angular/fire auth
 * that is considered a sure API and should not be unit tested.
 * this service only allow to test ../store/effects/auth.effect.ts by mocking it.
 */
